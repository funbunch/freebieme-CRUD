<h1>CREATE POST and then DISPLAY detailed ITEM</h1>
<img src="<%= imgSrc %>" alt="">
<% if (!locals.username) { %>
  <h1 id="user"></h1>

  <script>
      let currentUser = localStorage.getItem('user')
      document.getElementById('user').innerText = `👋 Hello, ${currentUser}`
      document.getElementById('userInput').value = currentUser
      console.log(`User logged in: ${currentUser}`)
  </script>
<% } else { %>
  <h1>👋 Hello, <%= username %></h1>

  <form enctype="multipart/form-data" action="/banana" method="POST">
    <input type="file" name="itemImg">
    <input type="submit" class="btn btn-primary">
  </form>

  <h4>Upload image</h4>
  <% if (!imgSrc) { %>

    <img src="https://placekitten.com/200/300" alt="">
    
 <% } else { %> 
  <img src="<%= imgSrc %>" alt="">
  <% } %> 


  <p>
      <form action='/location' method="GET">
          <input hidden name="user" value="<%= username %>">
          <button type="submit">Find your location 📍</button>
      </form>
  </p>
  <script>
      if (!localStorage.getItem('user')) {
          let user = "<%- username %>"
          localStorage.setItem('user', user)
      }
      
      let currentUser = localStorage.getItem('user')
      console.log(`User logged in: ${currentUser}`)
  </script>
<% } %>